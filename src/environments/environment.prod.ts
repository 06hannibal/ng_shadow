// import {HttpHeaders} from '@angular/common/http';

export const environment = {
  production: true,
  mainUrl : 'http://localhost:4200/',
  // httpjson : {headers: new HttpHeaders({
  //           'Content-Type': 'application/json'
  //       })}
};
